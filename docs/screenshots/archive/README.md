# Screenshot archive

One folder per release. Every image in `docs/screenshots` is a recording of the
real program, so replacing them loses what the program looked like at the time —
these are kept instead.

| | |
|---|---|
| [`0.3.1/`](0.3.1/) | before the window's dials carried three time constants, before the cascade's interleave tier was capped to four seconds of arrival, and before the panel put its version in the corner |

See [Reference](../../reference.md#the-screenshots) for how they are made.
